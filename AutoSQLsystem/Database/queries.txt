User tabel:


CREATE TABLE  "SQLUSERS" 
   (	"EMAIL" VARCHAR2(4000) NOT NULL ENABLE, 
	"PASSWORD" VARCHAR2(4000) NOT NULL ENABLE, 
	"NAME" VARCHAR2(4000) NOT NULL ENABLE, 
	"PHNO" NUMBER NOT NULL ENABLE, 
	 CONSTRAINT "SQLUSERS_PK" PRIMARY KEY ("EMAIL") ENABLE
    }    


CREATE TABLE  "CREATEDTABLES" 
   (	"TABLEID" NUMBER NOT NULL ENABLE, 
	"EMAIL" VARCHAR2(4000), 
	"TABLENAME" VARCHAR2(4000), 
	 CONSTRAINT "USERS_PK" PRIMARY KEY ("TABLEID") ENABLE
   ) ;ALTER TABLE  "CREATEDTABLES" ADD CONSTRAINT "USERS_FK" FOREIGN KEY ("EMAIL")
	  REFERENCES  "SQLUSERS" ("EMAIL") ENABLE;



CREATE TABLE  "UPLOADEDTABLES" 
   (	"TABLEID" NUMBER, 
	"TABLE_NAME" VARCHAR2(100), 
	"USER_EMAIL" VARCHAR2(50), 
	 CONSTRAINT "UPLOADEDTABLES_PK" PRIMARY KEY ("TABLEID") ENABLE
   ) ;ALTER TABLE  "UPLOADEDTABLES" ADD CONSTRAINT "UPLOADEDTABLES_FK" FOREIGN KEY ("USER_EMAIL")
	  REFERENCES  "SQLUSERS" ("EMAIL") ENABLE;